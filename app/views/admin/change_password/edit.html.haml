- content_for :title, t('.title')
= simple_form_for(@user, url: admin_change_password_path) do |f|
  = f.error_notification
  .form-inputs
    = f.input :old_password
    = f.input :password
    = f.input :password_confirmation
  .form-actions
    = f.button :submit
