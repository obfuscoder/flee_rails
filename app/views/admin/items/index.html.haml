- content_for :title, t('.title')
%table.table.table-condensed.table-hover.table-striped
  %tr
    %th= t('position')
    %th= Item.human_attribute_name :category
    %th= Item.human_attribute_name :description
    %th= Item.human_attribute_name :size
    %th= Item.human_attribute_name :price
    %th= link_to 'Artikel hinzufügen', new_admin_seller_item_path(@seller)
  - @items.each_with_index do |item, index|
    %tr
      %td= index+1
      %td= item.category
      %td= item.description
      %td= item.size
      %td= item.price
      %td
        - if item.labels.empty?
          = link_to t('edit'), edit_admin_seller_item_path(@seller, item)
        - else
          #{item.labels.first.code}
        = link_to t('destroy'), admin_seller_item_path(@seller, item), method: :delete, data: { confirm: t('.destroy.confirm') }
